package phases;

import sim.field.grid.IntGrid2D;

public interface ECInterface
{
	public IntGrid2D gimmePopulationGrid();		// getters show up in the model tab of the GUI, so lets call it gimme
	public int gimmeRunDuration();
}
